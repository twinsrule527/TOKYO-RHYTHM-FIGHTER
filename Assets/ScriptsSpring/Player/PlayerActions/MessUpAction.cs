using System.Collections;
using System.Collections.Generic;
using UnityEngine;
//This action is performed whenever the player fails to make an action successfully, to stop them from making other actions
public class MessUpAction : PlayerAction
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
